# Keep API compatibility with existing Streamlit client
server.port=8080

# These are informational in this minimal-change migration.
# The app's JDBC layer reads connection info via env vars / system props in dao.DBConnection.
#
# spring.datasource.url=jdbc:mysql://localhost:3306/unisync
# spring.datasource.username=root
# spring.datasource.password=

spring.thymeleaf.cache=false

